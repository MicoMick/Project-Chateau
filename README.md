# Project-Chataeu

## Mobile App

The Flutter mobile client lives in [`mobile/`](mobile/). See [mobile/README.md](mobile/README.md) for setup and project structure, and [mobile/ANDROID_SETUP.md](mobile/ANDROID_SETUP.md) for Android-specific instructions (SDK setup, emulator, physical device, release builds).

```bash
cd mobile
flutter pub get
flutter run -d chrome
```