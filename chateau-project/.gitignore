# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code session lock file (local/machine-specific, regenerates automatically)
.claude/scheduled_tasks.lock

# Environment variables (contains Supabase keys — never commit)
.env
.env.local

# Vite cache
.vite

# Supabase CLI local cache (e.g. latest-version check) — regenerates on every run
frontend/supabase/.temp/
